# Скопировать в .env и заполнить. .env в git НЕ коммитится (в .gitignore).
# Домен с A-записью на IP этого сервера:
DOMAIN=example.com
# Секрет админки /admin/ (сгенерировать: openssl rand -hex 32):
ADMIN_TOKEN=change-me
