# Этап «Арсенал» — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Оружейный магазин, лесенка из трёх оружий (бита → пистолет → винтовка), патроны-расходник и хитскан-стрельба — деньги обретают смысл, бой становится азартным.

**Architecture:** Единый источник правды баланса — `shared/src/config.ts` (`WEAPONS`). Сервер валидирует всё: покупки (дистанция/деньги), выстрелы (дальность + конус + LOS по AABB зданий, кулдаун, патроны). Хитскан — мгновенный луч 2D по `segmentHitsAABB`. Оружие сгорает при смерти и аресте. Клиент: HTML-диалог магазина по образцу сейфа, tracer-визуал по broadcast `'shot'`.

**Tech Stack:** Colyseus 0.16 (сервер, 20 Гц), better-sqlite3, Three.js + Vite (клиент), Vitest.

**Spec:** `docs/superpowers/specs/2026-07-18-arsenal-design.md` (утверждена).

## Global Constraints

- Баланс правится ТОЛЬКО в `shared/src/config.ts`; кулачные константы `PUNCH_*` не меняются.
- Стрельба только `mode === 'foot'` (drive-by не входит). Целью может быть любой живой игрок (`mode` не `'dead'`/`'jail'`), включая водителя — машина укрытием не является.
- Одно оружие в руках: покупка нового заменяет старое без возврата денег. Инвентаря нет.
- Невалидные сообщения молча игнорируются (как сейчас в CityRoom).
- Коммиты на русском, conventional-style (`feat(server): ...`), как в истории репо.
- Тестовые гейты каждой задачи: `npm test --workspace=<pkg> -- <filter>`; финальный гейт — Task 11.
- **Отступления от спеки (согласовать при ревью плана):**
  1. `handleAttack` получает 5-й параметр `colliders: AABB[]` и возвращает `Shot | null` — спека говорит «сигнатура не меняется», но LOS нужны коллайдеры, а broadcast `'shot'` делает CityRoom по возвращённому значению.
  2. `'shot'` рассылается на КАЖДЫЙ выстрел (не только попадание) — в payload есть `hit: boolean` и добавлен `victim: string` (sessionId цели, `''` при промахе) для клиентской виньетки.
  3. Координаты `gunShop` конкретизированы: `{ x: 30, z: -50 }` — западная стена дома квартала (2,1), у дороги x=0.

---

### Task 1: Shared — конфиг оружия + `segmentHitsAABB`

**Files:**
- Modify: `shared/src/config.ts`
- Modify: `shared/src/physics.ts`
- Test: `shared/test/physics.test.ts`

**Interfaces:**
- Produces: `WEAPONS` (`{ bat, pistol, rifle }`, поля `name, price, damage, range, cooldownMs, ranged`), `type WeaponKind = 'bat' | 'pistol' | 'rifle'`, `AMMO_PACK_PRICE = 100`, `AMMO_PACK_SIZE = 30`, `AMMO_MAX = 300`, `segmentHitsAABB(x1, z1, x2, z2, b: AABB): boolean`. Экспортируются через `shared/src/index.ts` (уже `export *`).

- [ ] **Step 1: Failing test — `segmentHitsAABB`**

Добавить в `shared/test/physics.test.ts` (импорт дополнить `segmentHitsAABB`):

```ts
describe('segmentHitsAABB', () => {
  // wall = { x: 10, z: 0, w: 2, d: 10 } → x: 9..11, z: -5..5 (уже объявлен в файле)
  it('отрезок через стену пересекает её', () => {
    expect(segmentHitsAABB(0, 0, 20, 0, wall)).toBe(true);
  });
  it('касание грани считается пересечением', () => {
    expect(segmentHitsAABB(0, 0, 9, 0, wall)).toBe(true);
  });
  it('отрезок мимо стены не пересекает', () => {
    expect(segmentHitsAABB(0, 10, 20, 10, wall)).toBe(false);
  });
  it('отрезок, заканчивающийся перед стеной, не пересекает', () => {
    expect(segmentHitsAABB(0, 0, 8, 0, wall)).toBe(false);
  });
  it('отрезок внутри стены пересекает', () => {
    expect(segmentHitsAABB(9.5, 0, 10.5, 0, wall)).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --workspace=shared -- physics`
Expected: FAIL — `segmentHitsAABB is not a function` (или ошибка импорта).

- [ ] **Step 3: Implement — конфиг + функция**

В `shared/src/config.ts` после блока машин (перед `PUNCH_RANGE`) вставить:

```ts
export const WEAPONS = {
  bat:    { name: 'Бита',     price: 150,  damage: 35, range: 2.5, cooldownMs: 800, ranged: false },
  pistol: { name: 'Пистолет', price: 600,  damage: 15, range: 40,  cooldownMs: 400, ranged: true  },
  rifle:  { name: 'Винтовка', price: 2000, damage: 12, range: 80,  cooldownMs: 120, ranged: true  },
} as const;
export type WeaponKind = keyof typeof WEAPONS; // 'bat' | 'pistol' | 'rifle'
export const AMMO_PACK_PRICE = 100;
export const AMMO_PACK_SIZE = 30;
export const AMMO_MAX = 300;
```

В `shared/src/physics.ts` в конец файла:

```ts
// 2D-пересечение отрезка с AABB (slab-метод). Касание грани = пересечение.
export function segmentHitsAABB(x1: number, z1: number, x2: number, z2: number, b: AABB): boolean {
  const minX = b.x - b.w / 2, maxX = b.x + b.w / 2;
  const minZ = b.z - b.d / 2, maxZ = b.z + b.d / 2;
  let tmin = 0, tmax = 1;
  for (const [p, d, lo, hi] of [[x1, x2 - x1, minX, maxX], [z1, z2 - z1, minZ, maxZ]] as const) {
    if (d === 0) {
      if (p < lo || p > hi) return false;
      continue;
    }
    let t1 = (lo - p) / d;
    let t2 = (hi - p) / d;
    if (t1 > t2) { const tmp = t1; t1 = t2; t2 = tmp; }
    tmin = Math.max(tmin, t1);
    tmax = Math.min(tmax, t2);
    if (tmin > tmax) return false;
  }
  return true;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm test --workspace=shared`
Expected: PASS, все файлы shared зелёные.

- [ ] **Step 5: Commit**

```bash
git add shared/src/config.ts shared/src/physics.ts shared/test/physics.test.ts
git commit -m "feat(shared): конфиг оружия WEAPONS/AMMO, segmentHitsAABB для хитскана"
```

---

### Task 2: Shared — точка `gunShop` на карте

**Files:**
- Modify: `shared/src/map.ts:16-25` (интерфейс `CityMap`) и `:73-87` (return)
- Test: `shared/test/map.test.ts`

**Interfaces:**
- Produces: `CityMap.gunShop: Point` = `{ x: 30, z: -50 }`. Потребляется Task 5 (магазин), Task 8 (interact), Task 9 (метка, подсказка).

- [ ] **Step 1: Failing test — точка покрыта проверкой «не внутри зданий»**

В `shared/test/map.test.ts` в тесте `'точки доставки и ключевые места не внутри зданий'` добавить `map.gunShop` в массив:

```ts
const pts = [...map.deliveryTargets, map.hospitalDoor, map.policeDoor, map.jailCell, map.warehouse, map.gunShop];
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --workspace=shared -- map`
Expected: FAIL — компиляция/рантайм: `gunShop` отсутствует в `CityMap`.

- [ ] **Step 3: Implement**

В `shared/src/map.ts` интерфейс `CityMap` дополнить строкой после `warehouse: Point;`:

```ts
  gunShop: Point;
```

В return-объект `createCityMap` после `warehouse: { x: -150, z: 127 },` добавить:

```ts
    gunShop: { x: 30, z: -50 }, // квартал (2,1), западная стена дома у дороги x=0
```

- [ ] **Step 4: Run tests**

Run: `npm test --workspace=shared`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add shared/src/map.ts shared/test/map.test.ts
git commit -m "feat(shared): точка gunShop на карте (квартал 2,1)"
```

---

### Task 3: Server — поля `weapon`/`ammo` в схеме Player

**Files:**
- Modify: `server/src/schema/GameState.ts:4-22`
- Test: `server/test/schema.test.ts`

**Interfaces:**
- Produces: `Player.weapon: string` (`''` = кулаки, иначе `WeaponKind`), `Player.ammo: number`. Потребляется Tasks 5–10.

- [ ] **Step 1: Failing test**

В `server/test/schema.test.ts` в `'has expected defaults'` добавить:

```ts
    expect(p.weapon).toBe('');
    expect(p.ammo).toBe(0);
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --workspace=server -- schema`
Expected: FAIL — `p.weapon` undefined.

- [ ] **Step 3: Implement**

В `server/src/schema/GameState.ts` класс `Player`, после поля `deliveryDeadline`:

```ts
  @type('string') weapon = ''; // '' = кулаки
  @type('number') ammo = 0;
```

- [ ] **Step 4: Run tests**

Run: `npm test --workspace=server -- schema`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add server/src/schema/GameState.ts server/test/schema.test.ts
git commit -m "feat(server): поля weapon/ammo в схеме Player"
```

---

### Task 4: Server — персистентность `weapon`/`ammo` + миграция БД

**Files:**
- Modify: `server/src/db.ts`
- Test: `server/test/db.test.ts`

**Interfaces:**
- Consumes: ничего из предыдущих задач (PlayerRecord независим от схемы).
- Produces: `PlayerRecord += { weapon: string; ammo: number }`; `GameDB` идемпотентно добавляет колонки при старте. Потребляется Task 8 (`onJoin`/`savePlayer`).

- [ ] **Step 1: Failing tests — round-trip и миграция**

В `server/test/db.test.ts`:
1. Импорт дополнить: `import Database from 'better-sqlite3';`
2. В существующих `toEqual` добавить `weapon: '', ammo: 0` (новый игрок) и `weapon: 'pistol', ammo: 90` / `weapon: '', ammo: 0` в save/load. Итоговый файл:

```ts
import { describe, it, expect, afterEach } from 'vitest';
import { unlinkSync, existsSync } from 'node:fs';
import Database from 'better-sqlite3';
import { GameDB } from '../src/db.js';
import { START_CASH } from '@mmo/shared';

function cleanup(path: string): void {
  for (const suffix of ['', '-wal', '-shm']) {
    if (existsSync(path + suffix)) unlinkSync(path + suffix);
  }
}

describe('GameDB', () => {
  let db: GameDB;
  afterEach(() => db?.close());

  it('новый игрок получает стартовые значения', () => {
    db = new GameDB(':memory:');
    const rec = db.load('alice');
    expect(rec).toEqual({ name: 'alice', cash: START_CASH, safe: 0, apt: '', kills: 0, deaths: 0, weapon: '', ammo: 0 });
  });

  it('save/load сохраняет прогресс, включая оружие', () => {
    db = new GameDB(':memory:');
    db.load('bob');
    db.save({ name: 'bob', cash: 777, safe: 200, apt: 'apt3', kills: 2, deaths: 1, weapon: 'pistol', ammo: 90 });
    expect(db.load('bob')).toEqual({ name: 'bob', cash: 777, safe: 200, apt: 'apt3', kills: 2, deaths: 1, weapon: 'pistol', ammo: 90 });
  });

  it('данные переживают переоткрытие файла', () => {
    const path = `test-${Date.now()}.db`;
    const db1 = new GameDB(path);
    db1.save({ name: 'c', cash: 1, safe: 2, apt: '', kills: 0, deaths: 0, weapon: 'bat', ammo: 0 });
    db1.close();
    const db2 = new GameDB(path);
    expect(db2.load('c').weapon).toBe('bat');
    db2.close();
    cleanup(path);
    db = new GameDB(':memory:'); // заглушка для afterEach
  });

  it('миграция: старая таблица без weapon/ammo дополняется идемпотентно', () => {
    const path = `test-migrate-${Date.now()}.db`;
    const raw = new Database(path);
    raw.exec(`CREATE TABLE players (
      name TEXT PRIMARY KEY, cash INTEGER NOT NULL, safe INTEGER NOT NULL,
      apt TEXT NOT NULL DEFAULT '', kills INTEGER NOT NULL DEFAULT 0, deaths INTEGER NOT NULL DEFAULT 0
    )`);
    raw.prepare(`INSERT INTO players (name, cash, safe, apt, kills, deaths) VALUES ('old', 100, 0, '', 0, 0)`).run();
    raw.close();

    db = new GameDB(path); // первая миграция
    expect(db.load('old')).toEqual({ name: 'old', cash: 100, safe: 0, apt: '', kills: 0, deaths: 0, weapon: '', ammo: 0 });
    db.close();
    db = new GameDB(path); // повторный запуск — миграция не падает
    expect(db.load('old').cash).toBe(100);
    db.close();
    cleanup(path);
    db = new GameDB(':memory:');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --workspace=server -- db`
Expected: FAIL — `toEqual` расходится (лишние/недостающие поля), миграция падает.

- [ ] **Step 3: Implement**

Полностью заменить `server/src/db.ts`:

```ts
import Database from 'better-sqlite3';
import { START_CASH } from '@mmo/shared';

export interface PlayerRecord {
  name: string;
  cash: number;
  safe: number;
  apt: string;
  kills: number;
  deaths: number;
  weapon: string;
  ammo: number;
}

export class GameDB {
  private db: Database.Database;

  constructor(path: string) {
    this.db = new Database(path);
    this.db.pragma('journal_mode = WAL');
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS players (
        name TEXT PRIMARY KEY,
        cash INTEGER NOT NULL,
        safe INTEGER NOT NULL,
        apt TEXT NOT NULL DEFAULT '',
        kills INTEGER NOT NULL DEFAULT 0,
        deaths INTEGER NOT NULL DEFAULT 0
      )
    `);
    this.migrate();
  }

  // идемпотентно: добавляет колонки, которых нет (старые БД MVP)
  private migrate(): void {
    const cols = this.db.prepare('PRAGMA table_info(players)').all() as { name: string }[];
    const has = (n: string) => cols.some(c => c.name === n);
    if (!has('weapon')) this.db.exec(`ALTER TABLE players ADD COLUMN weapon TEXT NOT NULL DEFAULT ''`);
    if (!has('ammo')) this.db.exec(`ALTER TABLE players ADD COLUMN ammo INTEGER NOT NULL DEFAULT 0`);
  }

  load(name: string): PlayerRecord {
    const row = this.db.prepare('SELECT * FROM players WHERE name = ?').get(name) as PlayerRecord | undefined;
    if (row) return row;
    const rec: PlayerRecord = { name, cash: START_CASH, safe: 0, apt: '', kills: 0, deaths: 0, weapon: '', ammo: 0 };
    this.save(rec);
    return rec;
  }

  save(rec: PlayerRecord): void {
    this.db.prepare(`
      INSERT INTO players (name, cash, safe, apt, kills, deaths, weapon, ammo)
      VALUES (@name, @cash, @safe, @apt, @kills, @deaths, @weapon, @ammo)
      ON CONFLICT(name) DO UPDATE SET
        cash = @cash, safe = @safe, apt = @apt, kills = @kills, deaths = @deaths,
        weapon = @weapon, ammo = @ammo
    `).run(rec);
  }

  close(): void {
    this.db.close();
  }
}
```

- [ ] **Step 4: Run tests**

Run: `npm test --workspace=server -- db`
Expected: PASS (4 теста).

- [ ] **Step 5: Commit**

```bash
git add server/src/db.ts server/test/db.test.ts
git commit -m "feat(server): персистентность weapon/ammo, идемпотентная миграция БД"
```

---

### Task 5: Server — оружейный магазин `shop.ts`

**Files:**
- Create: `server/src/systems/shop.ts`
- Test: `server/test/shop.test.ts`

**Interfaces:**
- Consumes: `WEAPONS, WeaponKind, AMMO_*` (Task 1), `CityMap.gunShop` (Task 2), `Player.weapon/ammo` (Task 3), `DOOR_DIST` (существующая).
- Produces: `tryBuyWeapon(state, playerId, kind: string, map): 'ok' | 'too_far' | 'no_money' | 'bad_kind'`, `tryBuyAmmo(state, playerId, map): 'ok' | 'too_far' | 'no_money'`. Потребляется Task 8.

- [ ] **Step 1: Failing tests — все ветки**

Создать `server/test/shop.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { GameState, Player } from '../src/schema/GameState.js';
import { tryBuyWeapon, tryBuyAmmo } from '../src/systems/shop.js';
import { WEAPONS, AMMO_PACK_PRICE, AMMO_PACK_SIZE, AMMO_MAX, createCityMap } from '@mmo/shared';

const map = createCityMap();

function setup(cash = 5000) {
  const state = new GameState();
  const p = new Player();
  p.name = 'buyer';
  p.cash = cash;
  p.x = map.gunShop.x;
  p.z = map.gunShop.z;
  state.players.set('s1', p);
  return { state, p };
}

describe('оружейный магазин', () => {
  it('покупка оружия: деньги списаны, оружие в руках', () => {
    const { state, p } = setup();
    expect(tryBuyWeapon(state, 's1', 'bat', map)).toBe('ok');
    expect(p.weapon).toBe('bat');
    expect(p.cash).toBe(5000 - WEAPONS.bat.price);
  });

  it('новое оружие заменяет старое без возврата', () => {
    const { state, p } = setup();
    tryBuyWeapon(state, 's1', 'bat', map);
    expect(tryBuyWeapon(state, 's1', 'pistol', map)).toBe('ok');
    expect(p.weapon).toBe('pistol');
    expect(p.cash).toBe(5000 - WEAPONS.bat.price - WEAPONS.pistol.price);
  });

  it('too_far: далеко от магазина', () => {
    const { state, p } = setup();
    p.x = 0; p.z = 0;
    expect(tryBuyWeapon(state, 's1', 'bat', map)).toBe('too_far');
    expect(tryBuyAmmo(state, 's1', map)).toBe('too_far');
  });

  it('too_far: в машине покупать нельзя', () => {
    const { state, p } = setup();
    p.mode = 'car';
    expect(tryBuyWeapon(state, 's1', 'bat', map)).toBe('too_far');
  });

  it('no_money: не хватает денег', () => {
    const { state } = setup(100);
    expect(tryBuyWeapon(state, 's1', 'rifle', map)).toBe('no_money');
    expect(tryBuyAmmo(state, 's1', map)).toBe('ok'); // пачка стоит 100 — хватает ровно
  });

  it('bad_kind: несуществующее оружие', () => {
    const { state, p } = setup();
    expect(tryBuyWeapon(state, 's1', 'minigun', map)).toBe('bad_kind');
    expect(p.weapon).toBe('');
  });

  it('патроны: пачка добавляется, упирается в AMMO_MAX', () => {
    const { state, p } = setup();
    expect(tryBuyAmmo(state, 's1', map)).toBe('ok');
    expect(p.ammo).toBe(AMMO_PACK_SIZE);
    p.ammo = AMMO_MAX - 5;
    tryBuyAmmo(state, 's1', map);
    expect(p.ammo).toBe(AMMO_MAX);
    expect(p.cash).toBe(5000 - AMMO_PACK_PRICE * 2);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --workspace=server -- shop`
Expected: FAIL — модуль `shop.js` не найден.

- [ ] **Step 3: Implement**

Создать `server/src/systems/shop.ts`:

```ts
import {
  WEAPONS, AMMO_PACK_PRICE, AMMO_PACK_SIZE, AMMO_MAX, DOOR_DIST,
  dist2, type CityMap, type WeaponKind,
} from '@mmo/shared';
import type { GameState, Player } from '../schema/GameState.js';

export type BuyWeaponResult = 'ok' | 'too_far' | 'no_money' | 'bad_kind';
export type BuyAmmoResult = 'ok' | 'too_far' | 'no_money';

function buyerAtShop(state: GameState, playerId: string, map: CityMap): Player | null {
  const p = state.players.get(playerId);
  if (!p || p.mode !== 'foot') return null;
  if (dist2(p.x, p.z, map.gunShop.x, map.gunShop.z) > DOOR_DIST * DOOR_DIST) return null;
  return p;
}

export function tryBuyWeapon(state: GameState, playerId: string, kind: string, map: CityMap): BuyWeaponResult {
  const p = buyerAtShop(state, playerId, map);
  if (!p) return 'too_far';
  if (!(kind in WEAPONS)) return 'bad_kind';
  const w = WEAPONS[kind as WeaponKind];
  if (p.cash < w.price) return 'no_money';
  p.cash -= w.price;
  p.weapon = kind; // замена старого без возврата (спека 2)
  return 'ok';
}

export function tryBuyAmmo(state: GameState, playerId: string, map: CityMap): BuyAmmoResult {
  const p = buyerAtShop(state, playerId, map);
  if (!p) return 'too_far';
  if (p.cash < AMMO_PACK_PRICE) return 'no_money';
  p.cash -= AMMO_PACK_PRICE;
  p.ammo = Math.min(AMMO_MAX, p.ammo + AMMO_PACK_SIZE);
  return 'ok';
}
```

- [ ] **Step 4: Run tests**

Run: `npm test --workspace=server -- shop`
Expected: PASS (7 тестов).

- [ ] **Step 5: Commit**

```bash
git add server/src/systems/shop.ts server/test/shop.test.ts
git commit -m "feat(server): оружейный магазин — tryBuyWeapon/tryBuyAmmo"
```

---

### Task 6: Server — оружие в бою, хитскан

**Files:**
- Modify: `server/src/systems/combat.ts:1-44` (handleAttack)
- Test: `server/test/combat.test.ts`

**Interfaces:**
- Consumes: `WEAPONS, WeaponKind, segmentHitsAABB` (Task 1), `Player.weapon/ammo` (Task 3).
- Produces: `handleAttack(state, runtimes, attackerId, now, colliders: AABB[]): Shot | null`; `interface Shot { from: Point; to: Point; hit: boolean; victim: string }` (victim = sessionId цели, `''` при промахе). Потребляется Task 8. `killPlayer(state, runtimes, killerId, victimId, now)` — сигнатура без изменений.

- [ ] **Step 1: Failing tests — бита, пистолет, LOS**

В `server/test/combat.test.ts`:
1. Импорт дополнить: `WEAPONS` в список из `@mmo/shared`.
2. ВСЕ существующие вызовы `handleAttack(state, runtimes, 'a', 1000)` дополнить 5-м аргументом `[]` (7 мест — каждый `it`).
3. Добавить новые тесты в конец `describe('бой')`:

```ts
  it('бита: урон 35, дальность 2.5, кулдаун 800', () => {
    const { state, a, v, runtimes } = setup();
    a.weapon = 'bat';
    v.z = -2.2; // в пределах 2.5
    handleAttack(state, runtimes, 'a', 1000, []);
    expect(v.hp).toBe(MAX_HP - WEAPONS.bat.damage);
    handleAttack(state, runtimes, 'a', 1500, []); // кулдаун 800 не прошёл
    expect(v.hp).toBe(MAX_HP - WEAPONS.bat.damage);
  });

  it('бита не достаёт на 3 метрах', () => {
    const { state, a, v, runtimes } = setup();
    a.weapon = 'bat';
    v.z = -3;
    handleAttack(state, runtimes, 'a', 1000, []);
    expect(v.hp).toBe(MAX_HP);
  });

  it('пистолет без патронов не стреляет', () => {
    const { state, a, v, runtimes } = setup();
    a.weapon = 'pistol';
    a.ammo = 0;
    const shot = handleAttack(state, runtimes, 'a', 1000, []);
    expect(shot).toBeNull();
    expect(a.ammo).toBe(0);
    expect(v.hp).toBe(MAX_HP);
  });

  it('пистолет: попадание тратит патрон и наносит урон, shot.hit=true', () => {
    const { state, a, v, runtimes } = setup();
    a.weapon = 'pistol';
    a.ammo = 5;
    v.z = -20; // в пределах 40, точно по центру конуса
    const shot = handleAttack(state, runtimes, 'a', 1000, []);
    expect(a.ammo).toBe(4);
    expect(v.hp).toBe(MAX_HP - WEAPONS.pistol.damage);
    expect(shot).toEqual({ from: { x: 0, z: 0 }, to: { x: 0, z: -20 }, hit: true, victim: 'v' });
  });

  it('промах тратит патрон, shot.hit=false, to — точка на дальности', () => {
    const { state, a, runtimes } = setup();
    a.weapon = 'pistol';
    a.ammo = 5;
    const shot = handleAttack(state, runtimes, 'a', 1000, []);
    expect(a.ammo).toBe(4);
    expect(shot).toEqual({ from: { x: 0, z: 0 }, to: { x: 0, z: -40 }, hit: false, victim: '' });
  });

  it('пистолет не бьёт дальше своей дальности', () => {
    const { state, a, v, runtimes } = setup();
    a.weapon = 'pistol';
    a.ammo = 5;
    v.z = -41;
    handleAttack(state, runtimes, 'a', 1000, []);
    expect(v.hp).toBe(MAX_HP);
  });

  it('пистолет не бьёт вне узкого конуса (~12°)', () => {
    const { state, a, v, runtimes } = setup();
    a.weapon = 'pistol';
    a.ammo = 5;
    v.x = 7; v.z = -27; // dot ≈ 0.968 < 0.98
    handleAttack(state, runtimes, 'a', 1000, []);
    expect(v.hp).toBe(MAX_HP);
  });

  it('стена блокирует выстрел (LOS)', () => {
    const { state, a, v, runtimes } = setup();
    a.weapon = 'pistol';
    a.ammo = 5;
    v.z = -20;
    const wall = [{ x: 0, z: -10, w: 4, d: 2 }];
    const shot = handleAttack(state, runtimes, 'a', 1000, wall);
    expect(v.hp).toBe(MAX_HP);
    expect(shot?.hit).toBe(false);
  });

  it('водитель машины — легальная цель (машина не укрытие)', () => {
    const { state, a, v, runtimes } = setup();
    a.weapon = 'pistol';
    a.ammo = 5;
    v.mode = 'car';
    v.z = -20;
    handleAttack(state, runtimes, 'a', 1000, []);
    expect(v.hp).toBe(MAX_HP - WEAPONS.pistol.damage);
  });

  it('убийство из пистолета: розыск и счётчики как у кулаков', () => {
    const { state, a, v, runtimes } = setup();
    a.weapon = 'pistol';
    a.ammo = 5;
    v.hp = WEAPONS.pistol.damage;
    const now = 5000;
    handleAttack(state, runtimes, 'a', now, []);
    expect(v.mode).toBe('dead');
    expect(a.wantedUntil).toBe(now + WANTED_DURATION_MS);
    expect(runtimes.get('a')!.kills).toBe(1);
  });
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --workspace=server -- combat`
Expected: FAIL — сигнатура `handleAttack` без 5-го параметра, `shot` undefined.

- [ ] **Step 3: Implement**

В `server/src/systems/combat.ts` заменить импорт и `handleAttack` (остальное — `killPlayer`, `tickRespawn` — не трогать):

```ts
import {
  PUNCH_RANGE, PUNCH_DAMAGE, PUNCH_COOLDOWN_MS, MAX_HP,
  DEATH_CASH_LOSS, WANTED_DURATION_MS, RESPAWN_DELAY_MS,
  WEAPONS, segmentHitsAABB, dist2,
  type AABB, type CityMap, type Point, type WeaponKind,
} from '@mmo/shared';
import type { GameState } from '../schema/GameState.js';
import type { Runtime } from '../runtime.js';

export interface Shot {
  from: Point;
  to: Point;
  hit: boolean;
  victim: string; // sessionId цели, '' при промахе
}

export function handleAttack(
  state: GameState,
  runtimes: Map<string, Runtime>,
  attackerId: string,
  now: number,
  colliders: AABB[],
): Shot | null {
  const a = state.players.get(attackerId);
  const art = runtimes.get(attackerId);
  if (!a || !art || a.mode !== 'foot') return null;
  const w = a.weapon && a.weapon in WEAPONS ? WEAPONS[a.weapon as WeaponKind] : null;
  const ranged = w?.ranged === true;
  const range = w ? w.range : PUNCH_RANGE;
  const damage = w ? w.damage : PUNCH_DAMAGE;
  const cooldownMs = w ? w.cooldownMs : PUNCH_COOLDOWN_MS;
  if (now - art.lastAttackAt < cooldownMs) return null;
  if (ranged && a.ammo <= 0) return null;

  const fx = -Math.sin(a.rotY);
  const fz = -Math.cos(a.rotY);
  const minDot = ranged ? 0.98 : 0.3;
  let bestId = '';
  let bestD = range * range;
  state.players.forEach((t, id) => {
    if (id === attackerId) return;
    if (t.mode === 'jail' || t.mode === 'dead') return;
    const d2 = dist2(a.x, a.z, t.x, t.z);
    if (d2 > bestD || d2 === 0) return;
    const len = Math.sqrt(d2);
    const dot = ((t.x - a.x) / len) * fx + ((t.z - a.z) / len) * fz;
    if (dot < minDot) return;
    if (ranged && colliders.some(b => segmentHitsAABB(a.x, a.z, t.x, t.z, b))) return;
    bestId = id;
    bestD = d2;
  });
  art.lastAttackAt = now;

  if (ranged) {
    a.ammo -= 1;
    if (!bestId) {
      return { from: { x: a.x, z: a.z }, to: { x: a.x + fx * range, z: a.z + fz * range }, hit: false, victim: '' };
    }
    const victim = state.players.get(bestId)!;
    victim.hp -= damage;
    const vrt = runtimes.get(bestId);
    if (vrt) vrt.lastDamageAt = now;
    if (victim.hp <= 0) killPlayer(state, runtimes, attackerId, bestId, now);
    return { from: { x: a.x, z: a.z }, to: { x: victim.x, z: victim.z }, hit: true, victim: bestId };
  }

  if (!bestId) return null;
  const victim = state.players.get(bestId);
  const vrt = runtimes.get(bestId);
  if (!victim || !vrt) return null;
  victim.hp -= damage;
  vrt.lastDamageAt = now;
  if (victim.hp <= 0) killPlayer(state, runtimes, attackerId, bestId, now);
  return null;
}
```

- [ ] **Step 4: Run tests**

Run: `npm test --workspace=server -- combat`
Expected: PASS (старые 6 + новые 10).

- [ ] **Step 5: Commit**

```bash
git add server/src/systems/combat.ts server/test/combat.test.ts
git commit -m "feat(server): оружие в бою — хитскан с конусом, LOS и расходом патронов"
```

---

### Task 7: Server — конфискация оружия при смерти и аресте

**Files:**
- Modify: `server/src/systems/combat.ts` (`killPlayer`)
- Modify: `server/src/systems/police.ts:57-65` (блок ареста)
- Test: `server/test/combat.test.ts`, `server/test/police.test.ts`

**Interfaces:**
- Consumes: `Player.weapon/ammo` (Task 3). Ничего не производит для других задач.

- [ ] **Step 1: Failing tests**

В `server/test/combat.test.ts` добавить в конец `describe('бой')`:

```ts
  it('смерть сжигает оружие и патроны', () => {
    const { state, v, runtimes } = setup();
    v.weapon = 'rifle';
    v.ammo = 120;
    v.hp = PUNCH_DAMAGE;
    handleAttack(state, runtimes, 'a', 1000, []);
    expect(v.mode).toBe('dead');
    expect(v.weapon).toBe('');
    expect(v.ammo).toBe(0);
  });
```

В `server/test/police.test.ts` добавить в конец `describe('полиция')`:

```ts
  it('арест конфискует оружие и патроны', () => {
    const { state, crim, runtimes } = setup();
    crim.weapon = 'pistol';
    crim.ammo = 30;
    for (let i = 0; i < 3; i++) tickPolice(state, runtimes, 1000 + i * 1000, 1, map);
    expect(crim.mode).toBe('jail');
    expect(crim.weapon).toBe('');
    expect(crim.ammo).toBe(0);
  });
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --workspace=server -- combat police`
Expected: FAIL — weapon/ammo не обнуляются.

- [ ] **Step 3: Implement**

В `server/src/systems/combat.ts` в `killPlayer` после `victim.deliveryTarget = '';` добавить:

```ts
  victim.weapon = '';
  victim.ammo = 0;
```

В `server/src/systems/police.ts` в блоке ареста после `crim.deliveryTarget = '';` добавить:

```ts
    crim.weapon = '';
    crim.ammo = 0;
```

- [ ] **Step 4: Run tests**

Run: `npm test --workspace=server -- combat police`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add server/src/systems/combat.ts server/src/systems/police.ts server/test/combat.test.ts server/test/police.test.ts
git commit -m "feat(server): конфискация оружия при смерти и аресте"
```

---

### Task 8: Server — проводка CityRoom (сообщения, interact, join/save)

**Files:**
- Modify: `server/src/rooms/CityRoom.ts`
- Test: `server/test/room.integration.test.ts`

**Interfaces:**
- Consumes: `tryBuyWeapon/tryBuyAmmo` (Task 5), `handleAttack(..., colliders): Shot | null` (Task 6), `PlayerRecord.weapon/ammo` (Task 4), `CityMap.gunShop` (Task 2), `DOOR_DIST` (уже импортирован).
- Produces: сообщения `'buyWeapon' {kind}` / `'buyAmmo'` → ответ `'shopResult' {ok, reason}`; `'openShop'` по interact у gunShop; broadcast `'shot'` (Shot из Task 6). Клиент потребляет в Tasks 9–10.

- [ ] **Step 1: Failing tests — покупка и выстрел через живую комнату**

В `server/test/room.integration.test.ts` добавить в конец `describe`:

```ts
  it('покупка оружия через buyWeapon у магазина', async () => {
    const room = await testServer.createRoom<GameState>('city') as any;
    const client = await testServer.connectTo(room, { name: 'shoper', role: 'citizen' });
    const p = room.state.players.get(client.sessionId);
    const shop = createCityMap().gunShop;
    p.x = shop.x;
    p.z = shop.z;
    p.cash = 1000;
    let result: any = null;
    client.onMessage('shopResult', (msg) => { result = msg; });
    client.send('buyWeapon', { kind: 'pistol' });
    await new Promise(r => setTimeout(r, 200));
    expect(result).toEqual({ ok: true, reason: 'ok' });
    expect(p.weapon).toBe('pistol');
    expect(p.cash).toBe(400);
  });

  it('interact у магазина шлёт openShop', async () => {
    const room = await testServer.createRoom<GameState>('city') as any;
    const client = await testServer.connectTo(room, { name: 'shoper2', role: 'citizen' });
    const p = room.state.players.get(client.sessionId);
    const shop = createCityMap().gunShop;
    p.x = shop.x;
    p.z = shop.z;
    let opened = false;
    client.onMessage('openShop', () => { opened = true; });
    client.send('interact');
    await new Promise(r => setTimeout(r, 200));
    expect(opened).toBe(true);
  });

  it('выстрел через attack: урон жертве и broadcast shot', async () => {
    const room = await testServer.createRoom<GameState>('city') as any;
    const shooter = await testServer.connectTo(room, { name: 'sh1', role: 'citizen' });
    const victim = await testServer.connectTo(room, { name: 'v1', role: 'citizen' });
    const ps = room.state.players.get(shooter.sessionId);
    const pv = room.state.players.get(victim.sessionId);
    // открытая местность без зданий (середина дороги), стрелок смотрит в -z
    ps.x = 0; ps.z = 50; ps.rotY = 0; ps.weapon = 'pistol'; ps.ammo = 10;
    pv.x = 0; pv.z = 30;
    let shot: any = null;
    victim.onMessage('shot', (msg) => { shot = msg; });
    shooter.send('attack');
    await new Promise(r => setTimeout(r, 200));
    expect(pv.hp).toBeLessThan(100);
    expect(shot?.hit).toBe(true);
    expect(shot?.victim).toBe(victim.sessionId);
  });
```

Импорт `createCityMap` уже есть в файле — оставить как есть.

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test --workspace=server -- room.integration`
Expected: FAIL — `buyWeapon` молча игнорируется, `openShop` не приходит, `shot` нет.

- [ ] **Step 3: Implement — правки CityRoom.ts**

1. Импорт дополнить:

```ts
import { tryBuyWeapon, tryBuyAmmo } from '../systems/shop.js';
```

2. В `onCreate` рядом с остальными `onMessage` добавить:

```ts
    this.onMessage('buyWeapon', (client, data) => {
      const reason = tryBuyWeapon(this.state, client.sessionId, String(data?.kind ?? ''), this.map);
      client.send('shopResult', { ok: reason === 'ok', reason });
    });
    this.onMessage('buyAmmo', (client) => {
      const reason = tryBuyAmmo(this.state, client.sessionId, this.map);
      client.send('shopResult', { ok: reason === 'ok', reason });
    });
```

3. Заменить обработчик `'attack'`:

```ts
    this.onMessage('attack', (client) => {
      const shot = handleAttack(this.state, this.runtimes, client.sessionId, Date.now(), this.colliders);
      if (shot) this.broadcast('shot', shot);
    });
```

4. В `handleInteract` после `if (p.mode !== 'foot') return;` добавить проверку магазина ПЕРЕД циклом квартир:

```ts
    if (dist2(p.x, p.z, this.map.gunShop.x, this.map.gunShop.z) < DOOR_DIST * DOOR_DIST) {
      client.send('openShop');
      return;
    }
```

5. В `onJoin` после `p.safe = rec.safe;` добавить:

```ts
    p.weapon = rec.weapon;
    p.ammo = rec.ammo;
```

6. В `savePlayer` заменить объект на:

```ts
      this.db.save({
        name: p.name, cash: p.cash, safe: p.safe, apt: p.apt,
        kills: rt.kills, deaths: rt.deaths, weapon: p.weapon, ammo: p.ammo,
      });
```

- [ ] **Step 4: Run tests**

Run: `npm test --workspace=server`
Expected: PASS, все файлы (включая старые интеграционные).

- [ ] **Step 5: Commit**

```bash
git add server/src/rooms/CityRoom.ts server/test/room.integration.test.ts
git commit -m "feat(server): buyWeapon/buyAmmo/openShop в CityRoom, broadcast shot, сохранение оружия"
```

---

### Task 9: Client — окно магазина, HUD, метка, подсказка

**Files:**
- Modify: `client/index.html`
- Modify: `client/src/style.css`
- Modify: `client/src/ui.ts`
- Modify: `client/src/world.ts:88`

**Interfaces:**
- Consumes: `'openShop'`, `'shopResult' {ok, reason}` (Task 8), `WEAPONS, AMMO_PACK_PRICE, AMMO_PACK_SIZE` (Task 1), `CityMap.gunShop` (Task 2), `Player.weapon/ammo` (схема реплицируется автоматически).
- Produces: элементы `#shopDialog`, `#shopItems`, `#buyAmmoBtn`, `#shopClose`, `#toast` (id фиксированы, Task 10 добавляет `#vignette` отдельно).

- [ ] **Step 1: index.html — диалог и тост**

В `client/index.html` внутрь `#hud` после `#safeDialog` добавить:

```html
    <div id="shopDialog" class="hidden">
      <div>Оружейный магазин</div>
      <div id="shopItems"></div>
      <button id="buyAmmoBtn">Патроны +30 (100$)</button>
      <button id="shopClose">Закрыть</button>
    </div>
    <div id="toast" class="hidden"></div>
```

- [ ] **Step 2: style.css — стили диалога и тоста**

В `client/src/style.css` селектор `#safeDialog` расширить до `#safeDialog, #shopDialog`, и в конец добавить:

```css
#shopItems { display: flex; flex-direction: column; gap: 6px; }
.shopRow { display: flex; justify-content: space-between; align-items: center; gap: 12px; }
#toast {
  position: absolute; bottom: 140px; left: 50%; transform: translateX(-50%);
  color: #fff; font-size: 16px; background: rgba(0,0,0,.7);
  padding: 8px 18px; border-radius: 6px;
}
```

- [ ] **Step 3: world.ts — метка магазина**

После `mark(map.warehouse, 0xff8800);` добавить:

```ts
  mark(map.gunShop, 0xcc44ff);
```

- [ ] **Step 4: ui.ts — логика магазина, HUD, подсказка**

1. Импорт заменить на:

```ts
import {
  DOOR_DIST, CAR_ENTER_DIST, DELIVERY_PICKUP_DIST, RENT_PRICE,
  WEAPONS, AMMO_PACK_PRICE, AMMO_PACK_SIZE,
  dist2, type CityMap, type WeaponKind,
} from '@mmo/shared';
```

2. Поля класса дополнить:

```ts
  private shopDialog = document.getElementById('shopDialog')!;
  private toast = document.getElementById('toast')!;
  private toastTimer = 0;
```

3. В конструктор после блока сейфа добавить:

```ts
    room.onMessage('openShop', () => this.shopDialog.classList.remove('hidden'));
    room.onMessage('shopResult', (msg: any) => {
      const texts: Record<string, string> = {
        ok: 'Куплено', too_far: 'Подойди ближе к магазину',
        no_money: 'Не хватает денег', bad_kind: 'Нет такого оружия',
      };
      this.showToast(texts[msg.reason] ?? 'Ошибка покупки');
    });
    const items = document.getElementById('shopItems')!;
    for (const kind of Object.keys(WEAPONS) as WeaponKind[]) {
      const w = WEAPONS[kind];
      const row = document.createElement('div');
      row.className = 'shopRow';
      const label = document.createElement('span');
      label.textContent = `${w.name} — урон ${w.damage}, дальность ${w.range} м`;
      const btn = document.createElement('button');
      btn.textContent = `${w.price}$`;
      btn.addEventListener('click', () => room.send('buyWeapon', { kind }));
      row.append(label, btn);
      items.append(row);
    }
    document.getElementById('buyAmmoBtn')!.textContent = `Патроны +${AMMO_PACK_SIZE} (${AMMO_PACK_PRICE}$)`;
    document.getElementById('buyAmmoBtn')!.addEventListener('click', () => room.send('buyAmmo'));
    document.getElementById('shopClose')!.addEventListener('click', () => this.shopDialog.classList.add('hidden'));
    this.shopDialog.addEventListener('click', (e) => (e.target as HTMLElement).blur());
```

4. Метод `showToast` добавить в класс:

```ts
  private showToast(text: string): void {
    this.toast.textContent = text;
    this.toast.classList.remove('hidden');
    clearTimeout(this.toastTimer);
    this.toastTimer = window.setTimeout(() => this.toast.classList.add('hidden'), 2000);
  }
```

5. В `update()` строку stats заменить на (оружие третьей строкой):

```ts
    const w = me.weapon && me.weapon in WEAPONS ? WEAPONS[me.weapon as WeaponKind] : null;
    const weaponLine = `Оружие: ${w ? w.name : 'Кулаки'}${w?.ranged ? ` · ${me.ammo}` : ''}`;
    this.stats.textContent =
      `HP: ${Math.ceil(me.hp)}  |  Наличные: ${me.cash}$  |  Сейф: ${me.safe}$\n` +
      `${roleRu}${me.apt ? `  |  Квартира: ${me.apt}` : ''}\n` +
      weaponLine;
```

6. В `computePrompt` после `if (me.mode !== 'foot') return '';` добавить:

```ts
    if (dist2(me.x, me.z, this.map.gunShop.x, this.map.gunShop.z) < DOOR_DIST * DOOR_DIST) {
      return 'E — оружейный магазин';
    }
```

- [ ] **Step 5: Verify build**

Run: `npm run build -w client`
Expected: `tsc --noEmit` и `vite build` зелёные.

- [ ] **Step 6: Commit**

```bash
git add client/index.html client/src/style.css client/src/ui.ts client/src/world.ts
git commit -m "feat(client): окно оружейного магазина, строка оружия в HUD, метка gunShop"
```

---

### Task 10: Client — визуал боя (tracer, ствол, виньетка, звук)

**Files:**
- Modify: `client/index.html`, `client/src/style.css`
- Modify: `client/src/avatars.ts`
- Create: `client/src/effects.ts`
- Modify: `client/src/main.ts:51-68`

**Interfaces:**
- Consumes: broadcast `'shot'` = `Shot { from, to, hit, victim }` (Task 8), `WEAPONS` (Task 1), `Player.weapon` (схема).
- Produces: `class Effects { constructor(scene, room); update(): void }` — instанс в main.ts.

- [ ] **Step 1: index.html + css — виньетка**

В `#hud` добавить:

```html
    <div id="vignette" class="hidden"></div>
```

В `style.css` в конец:

```css
#vignette {
  position: fixed; inset: 0; pointer-events: none; z-index: 8;
  box-shadow: inset 0 0 80px 40px rgba(255, 0, 0, .55);
}
```

- [ ] **Step 2: avatars.ts — ствол в руке**

1. Импорт: `import { WEAPONS, type WeaponKind } from '@mmo/shared';` (после импорта three).
2. `interface PlayerMesh` дополнить полем `gun: THREE.Mesh;`
3. В `makePlayerMesh` перед `return` добавить:

```ts
  const gun = new THREE.Mesh(
    new THREE.BoxGeometry(0.12, 0.12, 0.7),
    new THREE.MeshLambertMaterial({ color: 0x222222 }),
  );
  gun.position.set(0.45, 1.2, -0.35);
  gun.visible = false;
  group.add(gun);
```

и `return { group, body, head, marker, gun };`
4. В `update()` в блоке видимости игрока после `mesh.head.visible = onFoot;` добавить:

```ts
      const w = p.weapon && p.weapon in WEAPONS ? WEAPONS[p.weapon as WeaponKind] : null;
      mesh.gun.visible = onFoot && w?.ranged === true;
```

- [ ] **Step 3: effects.ts — tracer, виньетка, звук**

Создать `client/src/effects.ts`:

```ts
import * as THREE from 'three';
import type { Room } from 'colyseus.js';

const TRACER_MS = 80;
const VIGNETTE_MS = 150;

interface Tracer { line: THREE.Line; bornAt: number }

export class Effects {
  private tracers: Tracer[] = [];
  private vignette = document.getElementById('vignette')!;
  private vignetteTimer = 0;
  private audio: AudioContext | null = null;

  constructor(private scene: THREE.Scene, room: Room) {
    room.onMessage('shot', (msg: any) => this.onShot(room.sessionId, msg));
  }

  private onShot(myId: string, msg: { from: { x: number; z: number }; to: { x: number; z: number }; victim: string }): void {
    const geo = new THREE.BufferGeometry().setFromPoints([
      new THREE.Vector3(msg.from.x, 1.2, msg.from.z),
      new THREE.Vector3(msg.to.x, 1.2, msg.to.z),
    ]);
    const line = new THREE.Line(geo, new THREE.LineBasicMaterial({ color: 0xffee88, transparent: true }));
    this.scene.add(line);
    this.tracers.push({ line, bornAt: performance.now() });
    if (msg.victim === myId) {
      this.vignette.classList.remove('hidden');
      clearTimeout(this.vignetteTimer);
      this.vignetteTimer = window.setTimeout(() => this.vignette.classList.add('hidden'), VIGNETTE_MS);
    }
    this.click();
  }

  // опциональный щелчок без ассетов (спека 5)
  private click(): void {
    try {
      this.audio ??= new AudioContext();
      const osc = this.audio.createOscillator();
      const gain = this.audio.createGain();
      gain.gain.setValueAtTime(0.08, this.audio.currentTime);
      gain.gain.exponentialRampToValueAtTime(0.001, this.audio.currentTime + 0.06);
      osc.connect(gain).connect(this.audio.destination);
      osc.start();
      osc.stop(this.audio.currentTime + 0.06);
    } catch { /* звук опционален, без него играбельно */ }
  }

  update(): void {
    const now = performance.now();
    for (let i = this.tracers.length - 1; i >= 0; i--) {
      const t = this.tracers[i];
      const k = 1 - (now - t.bornAt) / TRACER_MS;
      if (k <= 0) {
        this.scene.remove(t.line);
        t.line.geometry.dispose();
        (t.line.material as THREE.Material).dispose();
        this.tracers.splice(i, 1);
      } else {
        (t.line.material as THREE.LineBasicMaterial).opacity = k;
      }
    }
  }
}
```

- [ ] **Step 4: main.ts — подключить Effects**

1. Импорт: `import { Effects } from './effects.js';`
2. После `const ui = new UI(room, map, avatars);` добавить:

```ts
  const effects = new Effects(scene, room);
```

3. В `renderer.setAnimationLoop` после `avatars.update(dt);` добавить `effects.update();`

- [ ] **Step 5: Verify build**

Run: `npm run build -w client`
Expected: зелёные `tsc --noEmit` + `vite build`.

- [ ] **Step 6: Commit**

```bash
git add client/index.html client/src/style.css client/src/avatars.ts client/src/effects.ts client/src/main.ts
git commit -m "feat(client): tracer-выстрелы, ствол в руке, виньетка попадания, щелчок выстрела"
```

---

### Task 11: Финальный гейт

**Files:** нет (только проверки).

- [ ] **Step 1: Все тесты**

Run: `npm test` (из корня: shared + server)
Expected: все файлы PASS (shared 2, server 10; ~75 тестов).

- [ ] **Step 2: Типчек пакетов**

Run: `npx tsc --noEmit -p shared && npx tsc --noEmit -p server && npx tsc --noEmit -p client`
Expected: без ошибок.

- [ ] **Step 3: Сборка клиента**

Run: `npm run build -w client`
Expected: зелёная сборка.

- [ ] **Step 4: Смоук живого сервера**

```bash
npm run dev -w server &  # фоном
sleep 4
node .superpowers/sdd/probe-join.mjs ws://localhost:2567
# ожидание: "OK ws://localhost:2567: вошёл как deploy-check, ..."
kill %1
```

- [ ] **Step 5: Финальный коммит (если остались изменения)**

```bash
git status --short  # пусто = всё закоммичено задачами
```

**После этапа (вне плана, отдельным решением пользователя):** редеплой на 77.42.4.230, открытие портов 80/2567, совместный ручной чек-лист (10 пунктов MVP + оружейный блок). Пользователь уже откладывал деплой — не выполнять без явной команды.
